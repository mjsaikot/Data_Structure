/*Take two doubly linked lists as input and check if they are the same or not.*/
#include <bits/stdc++.h>

using namespace std;

int main()
{

    list<int> myList1;
    list<int> myList2;
    for ()
        return 0;
}